# Automated Tests

`tests/epic1/` contains verification tied specifically to the Epic 1
implementation. `tests/shared/` contains cross-epic and application-level test
infrastructure.

Frontend directories contain Vitest and React Testing Library suites. Backend
directories contain pytest suites.

Production implementation remains under `frontend/src/` and `backend/app/`.
